# NEXT.js 初试

## 新建一个Nextjs应用

### 安装依赖

```bash
yarn add react react-dom next
```

### package.json 配置启动

```json
{
  "scripts": {
    "dev": "next -p 3006",
    "build": "next build",
    "start": "next start"
  }
}
```

### 运行

```bash
yarn dev
```